using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandomMapGenerator : MonoBehaviour
{
    [SerializeField]
    private List<MapNode> _nodesToPlace;

    // Start is called before the first frame update
    void Awake()
    {
        
    }
}
